<?xml version="1.0" encoding="utf-8"?>
<!--
  Screen-off trigger + system-wide function-button capture.
  flagRequestFilterKeyEvents + canRequestFilterKeyEvents lets us receive KEYCODE_CAMERA
  from anywhere in the system (not just while MainActivity is foreground), so the hardware
  function button always routes to CaptureBridge regardless of the current top activity.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRequestFilterKeyEvents"
    android:canPerformGestures="false"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="0" />
