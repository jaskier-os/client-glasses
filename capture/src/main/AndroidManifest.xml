<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <queries>
        <package android:name="com.repository.glasses.filesync" />
    </queries>

    <application
        android:name=".CaptureApp"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:largeHeap="true"
        android:label="Capture">

        <!-- Allow TFLite GPU delegate to dlopen the vendor OpenCL driver;
             without this Android 7+ blocks /vendor/lib64/libOpenCL.so and
             the delegate silently falls back to CPU. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL-car.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false"/>

        <!-- The QNN HTP backend stub (libQnnHtpV73Stub.so) dlopens the Qualcomm
             CDSP fastRPC client to reach the Hexagon V73 NPU. These are public
             vendor libs (listed in /vendor/etc/public.libraries.txt) but the app's
             classloader namespace blocks /vendor/lib64 unless explicitly granted
             here (API 31+). Without this the stub load fails ("libcdsprpc.so not
             found"), device-create returns INVALID_CONFIG and denoise falls to CPU. -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false"/>
        <uses-native-library android:name="libadsprpc.so" android:required="false"/>
        <uses-native-library android:name="libsdsprpc.so" android:required="false"/>

        <service
            android:name=".CaptureService"
            android:exported="true"
            android:foregroundServiceType="camera">
            <intent-filter>
                <action android:name="com.repository.glasses.capture.BIND" />
            </intent-filter>
        </service>

        <receiver
            android:name=".boot.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
